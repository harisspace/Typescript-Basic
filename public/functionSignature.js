"use strict";
// example 1
// you can initiate variables with function 
// let func: Function;
let sayHello;
sayHello = (name, hello) => {
    console.log(`${name} say ${hello}`);
};
// example 2
let logDetails;
logDetails = (name, age) => {
    console.log(`${name} is ${age} years old`);
};
